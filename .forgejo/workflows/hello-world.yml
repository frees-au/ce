name: "hello world"

on:
  push:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v6

      - name: Test
        run: |
          echo "Ref:    ${{ FORGEJO_SHA }}"
          echo "Commit: ${{ FORGEJO_REF_NAME }}"
