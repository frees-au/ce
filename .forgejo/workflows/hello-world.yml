name: "hello world"

on:
  push:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v6

      - name: Test
        run: |
          echo "Ref:    ${{ env.FORGEJO_SHA }}"
          echo "Commit: ${{ env.FORGEJO_REF_NAME }}"
