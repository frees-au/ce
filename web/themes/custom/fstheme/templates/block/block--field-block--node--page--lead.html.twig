<div{{ attributes }}>
  {{ title_prefix }}
  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    <div class="text-2xl leading-9 mb-8">
      {{ content }}
    </div>
  {% endblock %}
</div>
