<ul class="lg:flex">
  {% for item in items %}
    <li>
      <a href="{{ item.url }}" class="inline-block py-6 lg:p-6 text-5xl leading-14 md:text-5xl md:leading-14 lg:text-2xl lg:leading-12 xl:text-2xl">{{ item.title }}</a>
    </li>
  {% endfor %}
</ul>
