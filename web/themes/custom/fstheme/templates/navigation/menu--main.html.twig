<ul class="lg:flex lg:justify-end lg:gap-8">
  {% for item in items %}
    <li>
      <a href="{{ item.url }}" class="inline-block py-2 text-3xl leading-8">{{ item.title }}</a>
    </li>
  {% endfor %}
</ul>
