<ul class="lg:flex">
  {% for item in items %}
    <li>
      <a href="{{ item.url }}" class="inline-block py-2 lg:p-6 text-4xl leading-8 lg:text-2xl lg:leading-12 xl:text-2xl">{{ item.title }}</a>
    </li>
  {% endfor %}
</ul>
