{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div {{ region_attributes.content }}>
      <div class="container px-6" data-region="main">
        <div class="container-content">
          {{ content.content }}
        </div>
      </div>
    </div>
  </div>
{% endif %}
