{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div {{ region_attributes.content }}>
      <div class="container">
        <div class="container-content">
          {{ content.content }}
        </div>
      </div>
    </div>
  </div>
{% endif %}
