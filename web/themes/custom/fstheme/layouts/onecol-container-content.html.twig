{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div {{ region_attributes.content }}>
      <div class="container page-default-container-span">
        {{ content.content }}
      </div>
    </div>
  </div>
{% endif %}
